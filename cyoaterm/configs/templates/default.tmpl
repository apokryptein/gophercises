===========================
{{.Title}}
===========================
{{range .Story}}
  {{printf "%s" .}}
{{end}}
---------------------------
{{if .Options}}
  {{range $i, $a := .Options}}
    Press '{{$i}}' to go to {{.Arc}}
    {{printf "%s" .Text}} 
  {{end}}
---------------------------
{{else}}
{{end}}

